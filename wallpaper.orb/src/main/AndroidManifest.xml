<manifest xmlns:android="http://schemas.android.com/apk/res/android" package="tabcomputing.wallpaper.orb">
    <application>
        <service
            android:name="tabcomputing.wallpaper.orb.Wallpaper"
            android:label="@string/orb_label"
            android:permission="android.permission.BIND_WALLPAPER"
            android:exported="true"
            android:enabled="false">
            <intent-filter>
                <action android:name="android.service.wallpaper.WallpaperService"/>
            </intent-filter>
            <meta-data
                android:name="android.service.wallpaper"
                android:label="@string/orb_label"
                android:thumbnail="@drawable/thumbnail"
                android:resource="@xml/orb_wallpaper">
            </meta-data>
        </service>
        <activity
            android:name="tabcomputing.wallpaper.orb.SettingsActivity"
            android:exported="true"
            android:label="@string/orb_label" >
        </activity>
    </application>
</manifest>
