<manifest xmlns:android="http://schemas.android.com/apk/res/android" package="tabcomputing.wallpaper.gradient">
    <application>
        <service
            android:name=".Wallpaper"
            android:label="@string/gradient_label"
            android:permission="android.permission.BIND_WALLPAPER"
            android:exported="true"
            android:enabled="false">
            <intent-filter>
                <action android:name="android.service.wallpaper.WallpaperService"/>
            </intent-filter>
            <meta-data
                android:name="android.service.wallpaper"
                android:label="@string/gradient_label"
                android:thumbnail="@drawable/gradient_thumbnail"
                android:resource="@xml/gradient_wallpaper">
            </meta-data>
        </service>
        <activity
            android:name=".SettingsActivity"
            android:exported="true"
            android:label="@string/gradient_label" >
        </activity>
    </application>
</manifest>
